# Earning Robot Backend

Secure server-side API layer for Earning Robot.

## Endpoints

- GET /health
- GET /api/config/status

Private API keys must be configured as Railway/server environment variables. Never put them in the frontend or GitHub Pages build.

Provider routes will be added after the required secret names and integrations are confirmed.
